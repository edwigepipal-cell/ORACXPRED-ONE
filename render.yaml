services:
  - type: web
    name: fifa-prediction-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn fifa1:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PORT
        value: 10000
